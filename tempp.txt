CREATE MODEL `your_project.your_dataset.your_model`
OPTIONS (
  model_type = 'BOOSTED_TREE_CLASSIFIER',
  input_label_cols = ['target_column']
)
AS
SELECT * 
FROM EXTERNAL_QUERY(
  'your_project.region-your_project',
  "SELECT * FROM `your_bucket.your_data_file`"
);
